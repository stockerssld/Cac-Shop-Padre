{
  "name": "E-commerce",
  "private": true,
  "dependencies": {
    "aos": "^2.3.4",
    "bulmaswatch": "^0.7.2",
    "jquery": "^3.5.0"
  },
  "scripts": {
    "db": "rails db:drop && rails db:create && rails db:migrate && rails db:seed",
    "start": "rails s"
  }
}
